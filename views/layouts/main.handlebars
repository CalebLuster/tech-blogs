<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Coolest Tech Blog</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css">
</head>
<body>
    <div class="wrapper">
        <header>
            <h2>
                <a href="/">HomePage</a>
            </h2>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">Dashboard</a>
                <button id="logout" class="button-no logout">Logout</button>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <div class="site-row">
            <div class="site-name">
                <h2>Tech Blog</h2>
            </div>
        </div>
        <main>
            {{{ body }}}
        </main>
    </div>

    {{#if loggedIn}}
    <script src="/public/javascript/logout.js"></script>
    {{/if}}
</body>
</html>